## 1. 数据模型与基础设施准备

- [x] 1.1 设计并创建文章原文表、结构化家教信息表、任务主表、任务执行日志表（含自动/手动来源标记）
- [x] 1.2 为发布时间、城市、区域、年级、科目、薪资字符串与任务状态建立必要索引并完成迁移脚本
- [x] 1.3 在 `bootstrap`/`service`/`infrastructure` 中补齐对应实体、Mapper、Repository 与配置绑定
- [x] 1.4 增加配置项：单公众号来源配置、调度开关与调度时间窗口（不引入 token 配置依赖）

## 2. 单公众号自动采集链路

- [x] 2.1 实现单公众号文章列表扫描能力，仅基于公开页面发现新文章链接
- [x] 2.2 实现文章增量识别逻辑，避免对同一来源文章重复创建采集任务
- [x] 2.3 实现文章正文抓取与原文落库，保存来源链接、原始内容、发布时间与采集时间
- [x] 2.4 实现每日自动调度入口，接入统一任务状态流转（`PENDING`/`RUNNING`/`SUCCESS`/`FAILED`）

## 3. 手动 URL 补采链路

- [x] 3.1 提供手动补采入口（crawler CLI），支持提交公众号文章 URL 并触发任务入队
- [x] 3.2 实现 URL 合法性与可访问性校验，返回统一错误码与错误信息
- [x] 3.3 复用自动采集同一任务模型与执行器，统一状态机、日志字段与统计口径
- [x] 3.4 实现手动补采幂等控制，短时间重复提交同一 URL 仅保留一个有效执行任务

## 4. 解析与标准化

- [x] 4.1 实现规则解析器，抽取城市、区域、年级、科目、地址、时间安排、薪资、老师要求、发布时间
- [x] 4.2 实现缺失字段容错：可识别字段入库、不可识别字段留空且不阻断记录落库
- [x] 4.3 实现薪资按原文字符串保存策略，不做数值化拆分
- [x] 4.4 为每个结构化字段保存可回溯原文片段与文章来源引用

## 5. H5 后端查询接口

- [x] 5.1 提供家教信息分页列表接口（`GET /h5/tutoring-info/page`），默认按发布时间倒序，返回统一 `Result<T>` 包装
- [x] 5.2 实现关键词检索（覆盖内容、年级、科目、地址、时间、薪资、老师要求等片段字段）
- [x] 5.3 实现组合筛选（按前端查询参数组合过滤：`contentKeyword/gradeKeyword/subjectKeyword/addressKeyword/timeKeyword/salaryKeyword/teacherKeyword`）
- [x] 5.4 实现排序能力（`sortOrder=desc|asc`，按发布时间排序）
- [ ] 5.5 详情接口（本期调整为范围外，当前由列表直接提供 `sourceUrl` 跳转原文）

## 6. Web 前端页面与交互

- [x] 6.1 实现列表页（Vue3 + Vite，默认最新优先、分页、空态、错误态）
- [x] 6.2 实现搜索与筛选交互（内容/年级/科目/地址/时间/薪资/教员）并支持回车触发
- [x] 6.3 实现排序切换（最新优先、最早优先）并与后端 `sortOrder` 对齐
- [ ] 6.4 详情页展示（本期调整为范围外，暂不单独建设详情路由）
- [x] 6.5 完成响应式适配与基础可用性校验（桌面优先，小屏降级布局）

## 7. 任务可观测与失败重试

- [x] 7.1 记录任务状态流转时间线，支持按任务查询执行轨迹
- [x] 7.2 记录机器可读错误类型与可读错误信息，区分采集失败与解析失败
- [x] 7.3 实现失败任务重试入口（`retry_failed_task`），保留原执行日志并追加重试日志
- [x] 7.4 实现按日期统计任务成功率（`daily_success_rate`），统一统计自动与手动任务

## 8. 测试与验收

- [x] 8.1 为自动采集与手动补采新增单元测试与集成测试，覆盖成功/失败/幂等路径
- [x] 8.2 为解析标准化新增回归测试样本，覆盖完整字段、缺失字段、非标准薪资表达
- [x] 8.3 为查询接口新增接口测试，覆盖分页、关键词、组合筛选、发布时间升降序契约
- [ ] 8.4 为前端关键流程补充端到端验收用例（当前以手工联调与构建验证为主）
- [x] 8.5 执行受影响模块测试并记录结果（`mvn -pl java/test -am test -Dsurefire.failIfNoSpecifiedTests=false`）

## 9. 范围守卫与发布准备

- [x] 9.1 在实现说明中明确本期排除项：去重、生命周期、失效判定、独立详情页与薪资数值排序
- [ ] 9.2 产出发布检查清单（配置项、调度开关、回滚策略、监控项）并与设计文档对齐
